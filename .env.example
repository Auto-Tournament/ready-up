# ReadyUp release tooling
#
# Used by: ./discord-webhook.sh (and ./release.sh unless SKIP_DISCORD_WEBHOOK=1)

# Discord webhook URL for release announcements
DISCORD_WEBHOOK_URL="https://discord.com/api/webhooks/..."

