// Ready Up esports overrides (optional, per server)
// Executed at the end of `ReadyUp/esports_live.cfg` (ruleset=valve). Per-match changes belong in
// the match config "overrides" instead: they are reported as "differs from Valve", this file is not.

