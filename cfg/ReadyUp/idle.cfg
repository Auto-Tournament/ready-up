// Ready Up idle baseline (MatchZy-style): keep the server in the warmup-friendly
// state from warmup.cfg (emulated warmup, CS2 warmup off).

exec ReadyUp/warmup.cfg
// Also set by warmup.cfg; kept here so an older warmup.cfg still leaves idle without a bomb.
mp_give_player_c4 0
